{
  "name": "yoda-scrum-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  },
  "scripts": {
    "start": "concurrently --kill-others \"npm run start-app\" \"npm run start-server\"",
    "start-app": "react-app-rewired start",
    "start-server": "node ./server/src/server.js",
    "build": "react-app-rewired build",
    "test": "jest"
  }
}
